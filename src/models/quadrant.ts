export type PostQuadrant = {
    name: string
    low_limit:number
    upper_limit:number
}

export type UpdateQuadrant = {
    id:number
    name: string
    low_limit:number
    upper_limit:number
}

export type GetQuadrant = {
    id:number
    name: string
    low_limit:number
    upper_limit:number
}